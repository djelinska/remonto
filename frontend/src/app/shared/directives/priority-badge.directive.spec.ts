import { PriorityBadgeDirective } from './priority-badge.directive';

describe('PriorityBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new PriorityBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
