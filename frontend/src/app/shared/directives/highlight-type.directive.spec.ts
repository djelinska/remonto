import { HighlightTypeDirective } from './highlight-type.directive';

describe('HighlightTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTypeDirective();
    expect(directive).toBeTruthy();
  });
});
